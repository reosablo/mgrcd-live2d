scripts:
  fmt:
    cmd: deno fmt
  lint:
    cmd: deno lint
  test:
    cmd: deno test
    allow:
      read: ./testdata
      write: ./testdata
  pre-commit:
    gitHook: pre-commit
    cmd:
      - vr fmt
      - vr lint
  pre-push:
    gitHook: pre-push
    cmd:
      - vr test
